[package]
name = "sporec-parser"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Spore language parser (source text → AST)"
repository.workspace = true
license.workspace = true

[dev-dependencies]
spore-typeck = { path = "../spore-typeck" }
